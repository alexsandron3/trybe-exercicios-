const factorialNumber = (number) => number === 0 ? 1 : number * factorialNumber(number -1);
  // if (number === 0) {
  //   return 1;
  // } else {
  //   return ;
  // }

// };
factorialNumber;
// factorialNumber(3);

// 0  = 0 + (4 * (4-1)) 
// 12 = 12 + ()
// 
// 